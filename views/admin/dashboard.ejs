<%- include('../layouts/header.ejs')-%>

<nav class="navbar navbar-expand-lg ">
    <div class="container-fluid">
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active " href="/admin/home" aria-current="page" href="#">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active">Our Cars</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="#">Exhibitions</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="#" >Media House</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="/admin/logout" >Logout</a>
          </li>
        </ul>
      </div>
      <a class="navbar-brand" href="#"><h1>Retro</h1></a>
    </div>
  </nav>

<section class="content-section">
<div class="container">
    <div class="row align-items-center">
        <div class="col-md-12 content-text ">
            <table width="100%">
                <tr>
                    <th>Name</th>
                    <th>Image</th>
                    <th>Email</th>
                    <th>Mobile</th>
                    <th>Actions</th>
                </tr>

                <%
                if(users.length>0){
                  for(let i=0 ; i<users.length ;i++){
                    %>
                    <tr>
                        <td><%= users[i].name %></td>
                        <td><img src="/static/userImages/<%= users[i].image %>" width="50px" height="60px" alt="<%= users[i].image %>"></td>
                        <td><%= users[i].email %></td>
                        <td><%= users[i].mobile %></td>
                        <td><a href="/admin/edit-user?id=<%= users[i]._id %>">Edit</a></td>
                        <td><a href="/admin/delete-user?id=<%= users[i]._id %>">Delete</a></td>
                    </tr>
                    <%
                  }
                }else{
                %>
                <tr>
                    <td colspan="5">User not found</td>
                </tr>
                <%
                }
                %>
            </table>
            <tr>
              <td>Add new user</td>
              <td><a href="/admin/new-user"><button class="btn btn-danger rounded-pill">Add</button></a></td>
            </tr>
        </div>
    </div>
</div>
</section>
<div class="user-logout text-center">
  <button type="button" class="btn btn-danger"><a  href="/admin/logout" >Logout</a></button>
</div>

<%- include('../layouts/footer.ejs')-%>